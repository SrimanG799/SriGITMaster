package com.git;

public class GitCommands {
	public static void main(String[] args) {
		System.out.println("welcome");
	}
//git add  is used stage the changes
	//git add --all it is save the all changes in all files at a time
	//git commit is used to save the changes in local repository
	//git commit -m "commit message"
	//git push origin master/main is saves the changes in git Repository
}
